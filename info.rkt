#lang info

(define version "0.1")
(define pkg-authors '(tn))

(define deps '("base"
               "rosette"
               "ocelot"))

(define name "emcsabac")
;(define scribblings '([]))
(define collection "emcsabac")
(define compile-omit-paths '("test"))
